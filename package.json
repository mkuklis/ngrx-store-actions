{
  "name": "ngrx-store-actions",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "author": "",
  "devDependencies": {
    "tslint": "^3.13.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  },
  "scripts": {
    "build_dist": "rm -rf dist && tsc",
    "prepublish": "npm run typings && npm run build_dist",
    "typings": "typings install"
  }
}
